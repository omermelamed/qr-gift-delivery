// Shared className strings for rows inside a KebabMenu dropdown, so every action
// menu (campaign header/cards, employee directory, team) stays visually consistent.
export const MENU_ITEM = 'w-full text-start px-3 py-2 rounded-lg text-sm font-medium text-zinc-700 hover-brand transition-colors'
export const MENU_ITEM_DANGER = 'w-full text-start px-3 py-2 rounded-lg text-sm font-medium text-red-600 hover:bg-red-50 transition-colors'
export const MENU_ITEM_DISABLED = 'w-full text-start px-3 py-2 rounded-lg text-sm font-medium text-zinc-300 cursor-not-allowed'
