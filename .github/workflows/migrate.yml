name: Apply database migrations

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'supabase/migrations/**'

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link project
        run: supabase link --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Sync migration history
        # The Supabase CLI stores only the numeric timestamp prefix (e.g.
        # "20240101000001") as the version, not the full filename. We
        # delete any stale/wrong-format records and insert just the timestamps
        # for the 6 migrations already applied to the DB, so db push only runs
        # the genuinely new ones (007-009).
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
        run: |
          SQL=$(cat << 'ENDSQL'
          CREATE SCHEMA IF NOT EXISTS supabase_migrations;
          CREATE TABLE IF NOT EXISTS supabase_migrations.schema_migrations (
            version text NOT NULL PRIMARY KEY
          );
          DELETE FROM supabase_migrations.schema_migrations
          WHERE version NOT IN (
            '20240101000001',
            '20240101000002',
            '20240101000003',
            '20240101000004',
            '20240101000005',
            '20240101000006',
            '20240101000007',
            '20240101000008',
            '20240101000009'
          );
          INSERT INTO supabase_migrations.schema_migrations (version) VALUES
            ('20240101000001'),
            ('20240101000002'),
            ('20240101000003'),
            ('20240101000004'),
            ('20240101000005'),
            ('20240101000006')
          ON CONFLICT (version) DO NOTHING;
          ENDSQL
          )
          PAYLOAD=$(jq -n --arg q "$SQL" '{"query": $q}')
          curl -sf -X POST \
            "https://api.supabase.com/v1/projects/${SUPABASE_PROJECT_REF}/database/query" \
            -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD"

      - name: Debug - show CLI migration status
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          echo "=== local files ==="
          ls supabase/migrations/
          echo "=== supabase migration list ==="
          supabase migration list 2>&1 || true

      - name: Apply new migrations
        run: supabase db push --yes
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
